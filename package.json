{
  "name": "splash-page",
  "version": "1.0.0",
  "description": "Splash page with links to music and photography",
  "scripts": {
    "start": "live-server --port=3000 --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
